
services:
  trax:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: trax-core:dev
    environment:
      - MODE=aaip_scenario
    volumes:
      - vectors:/app/vectors/generated
    tty: true
volumes:
  vectors: {}
