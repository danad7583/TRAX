[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "trax"
version = "0.1.0"
description = "Python bindings for the TRAX Rust cryptographic trust layer."
requires-python = ">=3.8"
license = { text = "Apache-2.0" }

[tool.maturin]
features = ["python"]
